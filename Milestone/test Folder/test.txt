/* Test 1
int l() {}

int g(int q, int p) {}

int f() {
    int x, y, u, v, w;
    x = l() + y * -u / (v - -w) + x - (y * l() + u) - y;

    w = g(-y, (x + y));
    u = g((x + y), w * y) - w;

    if (g((((x + y))), -w) > (g(x, y) + y) || g(u, v) != -w) {
        return -(((((((x)))))));
    }
    x = ((((---(x + 2) + 2)) * 3));

    if ((x + u) < 2 || -x < 3 && x * 2 / 3 < 5 || x < (6) && y == 1) ;
}
*/


int f(int a, int b, int c) {}

int l(int x, int y, int z) {}

int g() {
    int x, y, u, v, w;

    x = f(v, w, l(x,y,u));
    
    x = f(f(f(x, y, u), f(x,v,w), u), f(x,w,u), f(x, y, u));
    
}


